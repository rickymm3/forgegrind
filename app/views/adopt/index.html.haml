.container.mx-auto.mt-16
  .rounded-3xl.bg-gradient-to-r.from-emerald-500.via-indigo-500.to-blue-500.text-white.px-8.py-10.shadow-xl.mb-10
    %h1.text-3xl.font-bold.mb-3 Adopt a Legendary Egg
    %p.text-sm.max-w-2xl.text-emerald-50
      Discover new companions by investing your resources into rare eggs. Each egg contains a unique lineage with different hatch times, costs, and surprises.

  = turbo_frame_tag "adoption-feedback"
  = turbo_frame_tag "toast-container", class: "fixed top-6 right-6 z-40 flex flex-col items-end"

  .grid.grid-cols-1.lg:grid-cols-4.gap-8
    .space-y-6
      %h2.text-lg.font-semibold.text-slate-800 Your Resources
      = turbo_frame_tag "user-inventory" do
        = render "user_inventory", user_items: @user_items, user_stat: current_user.user_stat

      .bg-white.border.border-slate-200.rounded-2xl.p-5.shadow-sm
        %h3.text-sm.font-semibold.text-slate-700.uppercase.mb-2 Adoption Tips
        %ul.text-sm.text-slate-600.space-y-2
          %li Ensure you have enough Diamonds or items before adopting.
          %li Limited eggs rotate every season—grab them before they leave!
          %li Each egg hatches at a different rate; plan your incubations wisely.

    = turbo_frame_tag "egg-list", class: "lg:col-span-3" do
      .grid.grid-cols-1.md:grid-cols-2.xl:grid-cols-3.gap-8
        - @eggs.each do |egg|
          - can_afford = current_user.can_afford_egg?(egg)
          = render "egg_card", egg: egg, can_afford: can_afford
