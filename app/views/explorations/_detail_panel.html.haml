= turbo_frame_tag "exploration_detail" do
  - if generated_exploration
    = turbo_frame_tag dom_id(generated_exploration, :detail) do
      = render "explorations/zone_card",
               generated_exploration: generated_exploration,
               user_exploration: user_exploration,
               state: (user_exploration ? :active : :selection),
               requirement_progress: requirement_progress || [],
               requirement_groups: requirement_groups || {},
               available_pets: current_user.user_pets.active.includes(:learned_abilities, pet: :pet_types),
               selected_pet_ids: [],
               filters: {}
  - else
    .bg-white.rounded-2xl.shadow-sm.p-8.text-center.text-slate-500
      %h2.text-lg.font-semibold.text-slate-700 No expeditions available
      %p.text-sm.mt-2 Use the scout action to discover new exploration opportunities.
