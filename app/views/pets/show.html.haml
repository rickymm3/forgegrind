.border-t.px-6.py-4.bg-gray-50
  .font-semibold.text-gray-800.mb-1= @pet.name
  .text-sm.text-gray-600.mb-3= "Rarity: #{@pet.rarity.name} | Power: #{@pet.power}"
  = form_with url: equip_user_pet_path(current_user.user_pets.find_by(pet: @pet)), method: :post do
    = submit_tag "Equip", class: "w-full bg-blue-600 hover:bg-blue-700 text-white font-medium py-2 rounded-lg transition"
