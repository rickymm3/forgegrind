<h1 class="text-3xl font-bold text-slate-900 mb-1">Edit Pet</h1>
<p class="text-sm text-slate-600 mb-6">
  <%= "#{@user_pet.name.presence || @user_pet.pet.name} owned by #{@user_pet.user.email}" %>
</p>

<%= render "form", user_pet: @user_pet %>
